<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31) splash screen overrides. Bez ovog, system uzima
    adaptive icon foreground koji se seče u splash circle-u (figure pri ivici).
-->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Krug" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
    </style>

    <!-- API 31+ splash tema — koristi native android:* atribute jer sistem uzima
         direktno iz activity theme, ne prolazi kroz core-splashscreen compat sloj. -->
    <style name="Theme.Krug.Splash" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Krug</item>
    </style>
</resources>
