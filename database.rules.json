{
  "rules": {
    "locations": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['lat','lng','updatedAt'])",
        "lat":        { ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90" },
        "lng":        { ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180" },
        "accuracy":   { ".validate": "newData.isNumber()" },
        "batteryPct": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100" },
        "charging":   { ".validate": "newData.isBoolean()" },
        "paused":     { ".validate": "newData.isBoolean()" },
        "bearing":    { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 360" },
        "speed":      { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "updatedAt":  { ".validate": "newData.isNumber()" },
        "online":     { ".validate": "newData.isBoolean()" },
        "$other":     { ".validate": false }
      }
    },
    "sos": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['lat','lng','triggeredAt'])",
        "lat":         { ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90" },
        "lng":         { ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180" },
        "triggeredAt": { ".validate": "newData.isNumber()" },
        "message":     { ".validate": "newData.isString() || !newData.exists()" },
        "circleId":    { ".validate": "newData.isString() || !newData.exists()" },
        "senderName":  { ".validate": "newData.isString() && newData.val().length <= 64 || !newData.exists()" },
        "circleName":  { ".validate": "newData.isString() && newData.val().length <= 32 || !newData.exists()" },
        "$other":      { ".validate": false }
      }
    },
    "locationRequests": {
      "$targetUid": {
        ".read": "auth != null && auth.uid === $targetUid",
        "$requesterUid": {
          ".write": "auth != null && (auth.uid === $requesterUid || auth.uid === $targetUid)",
          ".validate": "newData.isNumber() || !newData.exists()"
        }
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
